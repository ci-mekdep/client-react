export type SessionType = {
  id: string
  user_id: string
  agent: string
  ip: string
  os: string
  os_version: string
  browser: string
  browser_version: string
  device: string
  iat: Date
  lat: Date
}
